# S-266-03+信创实机裁决：模式 A（agentbox）两平台实机终局（2026-09-08）

## Ubuntu 24.04（goalos-test——sudo 免密就位后装 profile 实机链）

**AppArmor profile 面（userns 解禁=有效）**：
- 正确形态=官方 bwrap 先例：`flags=(unconfined)+userns,`（Ubuntu 24.04 文档形态——
  禁 aa-enforce；complain 不满足内核检查）；confined 形态=逐条加规则的跑步机
  （proc/sys/usr/cgroup/network/sys_admin 逐拦——审计日志逐条实锤后放弃该形态）。
- 装载后实证：uid_map 写通（「0 0 1」写成功——#267 的 EPERM 面=已解）+clone 全
  命名空间创建成功（strace 实锤 CLONE_NEWUSER|NEWNS|NEWNET 族全建）。
- scripts/gen-apparmor-profile.sh=生成器入库（安装期动态路径规范化）。

**但模式 A 仍败——供应商级 fail-open 实锤（隔离变量=最小驱动）**：
纯 agentbox Manager 直接调用（零 GoalOS 代码面）：`mgr.Available()=true`+
`ExecArgs(/bin/touch ~/agb-probe.txt)`=exit 0+文件落 home=**边界失效**。
命名空间在+Landlock 施加在（strace 全链），agentbox 自身不收敛 home 写。
= 供应商级问题（非我方配置可修）→ 模式 A 在 Ubuntu 24.04=不可用登记；
模式 B=该平台唯一承载（已实证）。上游跟进面登记。

## 麒麟 V10 SP1（goalos-kylin-test——信创真机）

- 内核 5.15.0-52（非矩阵假设的 4.19）；userns 全通（真机窗口确认项 #1 闭环）
- **Landlock 缺席**（麒麟内核未编入——模式 B 不可建）
- **agentbox fail-open 同形态**（home 写探针沙箱内成功——Precheck 正确拦下）
- 双引擎全灭=受限档当前 fail-closed 不可用（诚实拒绝在位——零裸跑）
- **kysec 执行控制**：未签名二进制一律 EPERM exit 126（hello-world 对照实锤）；
  白名单=kysec_set -n exectl -v original <路径> 标签制；**生效有分钟级同步延迟窗**
  （新打标立即跑=126，数分钟后通——kysec-sync-daemon 异步传播实机实证）；
  信任目录运维面=kysec_set -r 递归打标。
- 信创原生路线登记：kysec 自研 LSM（exectl/devctl/netctl 族）作受限档承载面的
  可行性评估=研究方向（Jobs 已批准该路线）。

## 追加（同场深挖终态）

- **麒麟 LSM 栈定性**（/proc/cmdline+config 实锤）：`lsm=capability,kycp,kysec,box,kyextend`
  ——**无 AppArmor、无 Landlock**（`# CONFIG_SECURITY_LANDLOCK is not set`）；
  麒麟安全栈=kycp+kysec+box+kyextend（自研私有，公开文档缺席）。
- **Ubuntu 模式 A fail-open 归因修正（同日深挖——撤回「供应商级」定性）**：
  真根因=**我方从未调用 agentbox.MaybeSandboxInit()**（agentbox 沙箱子进程=re-exec
  自身二进制+环境标记；vendor 文档原话「call at the very beginning of main()」——
  我方 daemon main 与测试 TestMain 均未调用=子进程落入正常 main/m.Run()=
  fail-open 裸跑+递归跑全量测试）。「最小驱动直驱」实验同样没调它——
  当时的「供应商级」结论是我方接线缺失的误判，现已更正。
  **残留面**：补上 MaybeSandboxInit 后边界测试仍 fail-open（worker 起来了但
  landlock 未布防——478 次 ABI 查询/0 add_rule/0 restrict_self）——下一层归因面。
  **bwrap 路线（Jobs 2026-09-08 裁决）实证两机全绿**（见下）——agentbox 深调
  可能整体被 bwrap 取代而 moot。
- Ubuntu uid_map EPERM（#267 面）=AppArmor profile 解了（`0 0 1` 写通+clone 全
  命名空间族 strace 实锤）；agentbox 边界仍未生效的残留归因=上条更正块
  （我方 init 缺失为主线——landlock 未布防=下一层，供应商定性已撤回）。

## bwrap 集成实证（2026-09-08——Jobs 裁决路线：kysec 逆向停止+bwrap 集成+gVisor 评估+非核心免自研）

**bwrap=模式 C 的现成实现**（userns+bind-mount 遮蔽+pivot_root——全球审计的
数千行 C 单二进制；GoalOS Go 侧仅拼参数调起）。

麒麟 V10 SP1（goalos-kylin-test）：**bwrap 0.4.0 系统自带**（kysec 天然信任
OS 签名包——零安装零标签税）。全矩阵绿：
- spawn OK；fs 遮蔽（--tmpfs /home/goalos 后真身内容不可见+.ssh 不可见+
  沙箱内写入随 tmpfs 消亡=宿主零污染）✓；--unshare-net 网络全拒 ✓

Ubuntu 24.04（goalos-test）：apt 装 bubblewrap 0.9.0 + goalos-bwrap profile
（gen-apparmor-profile.sh --bwrap 生成：unconfined+userns+net_admin+setpcap+
sys_admin——实机实锤：默认 unprivileged_userns 兜底 profile 剥 bwrap 的
net_admin/setpcap=回环拉起 RTM_NEWADDR 被拒=bwrap 死）后全矩阵绿：
- fs 遮蔽 ✓；网络全拒 ✓；工作区 bind 可写+持久 ✓（同路径 bind 形态）

**gVisor 评估**：runsc=需 root 安装+重依赖；受限档需求 bwrap 已全覆盖——
gVisor=I4 级（自带内核）过重，不进入本路线（登记不采纳：超出受限档需求面）。

**结论**：bwrap=Linux 受限档新承载候选（模式 C 的现成实现）——双 Linux 机
实证全绿；bwrapProvider 引擎集成=下一工作包。
