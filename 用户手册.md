# GoalOS 用户手册

> **版本**: v0.3.1
> **最后更新**: 2026-08-27
> **适用版本**: GoalOS v0.3.x

## 修改记录

| 版本 | 日期 | 变更说明 | 依据 |
|------|------|---------|------|
| v0.3.1 | 2026-08-27 | 全面修订对齐现行规范：CLI 唯一软件入口（R-1123——删除浏览器/Web UI 路径）；命令体系对齐 04 命令行界面设计规范；风险等级 R0-R5（R-1114——旧 L 命名族废弃）；审批交互=CLI 命令形态（R-1323）；补修改记录表 | 会议 #245 / R-1577 |
| v0.2.2 | 2026-08-01 | v0.2.2 版手册（其后 Web UI/原型废弃，本版起全面重写） | — |

---

## 1. 产品简介

GoalOS 是面向人类目标的个人操作系统。你只需要说出"我要什么"——系统负责安全地实现它。

### 核心链路

```
你表述目标 → 系统生成任务计划 → 你确认计划 → 安全执行 → 实时可见 → 交付结果
```

### 不是

- 不是聊天机器人——你不是来闲聊的
- 不是 Workflow 引擎——你不需要画流程图
- 不是编程工具——你不需要写代码

### 关键能力

- **CompletionContract（完成契约）**：系统和你确认"什么叫完成"——不再盲目执行。每次执行前你会看到成功标准和验收条件
- **Flow 执行模板**：同一类目标每次按相同标准流程执行——结果可预测
- **Multi-LLM 交叉验证**：AI 产出的结果经过多个模型独立审查——更可信；意见分歧时如实告诉你，由你裁决
- **失败诚实**：执行失败时系统如实告知原因并给出可操作建议——不伪造成功。恢复方式=以新会话重做（保留已完成产物），仍失败则交给你决策
- **运行可观测**：CLI 随时查看每个 Action 的执行状态、审查分歧详情、审计日志

---

## 2. 安装与启动

### 2.1 系统要求

| 项目 | 最低要求 |
|------|---------|
| 操作系统 | macOS 12+ / Linux（内核 5.x+）/ Windows 10+ |
| 内存 | 8 GB RAM |
| 磁盘 | 500 MB 可用空间 |
| 网络 | 需要 LLM API 访问（Anthropic / OpenAI / Google / 本地 Ollama） |

### 2.2 安装

```bash
# 1. 从 releases 页面下载 goalos-daemon 和 goalos
# 2. 赋予执行权限（macOS/Linux）
chmod +x goalos-daemon goalos
# 3. 移动到 PATH
mv goalos-daemon goalos /usr/local/bin/
# 4. 验证安装
goalos --version
```

### 2.3 配置

```bash
# 设置 LLM API Key（按你的提供商）
export ANTHROPIC_API_KEY="your-anthropic-api-key"
export OPENAI_API_KEY="sk-..."

# 可选：使用本地 Ollama
export GOALOS_LLM_PROVIDER="ollama"
export GOALOS_LLM_MODEL="llama3"

# 配置文件（可选。默认值已可用）：~/.goalos/config/daemon.yaml
```

### 2.4 启动

```bash
# Daemon 随第一条 goalos 命令自动启动；也可显式管理：
goalos daemon status       # 查看 daemon 状态（PID/活跃 Goal 数）
goalos daemon restart      # 重启
goalos daemon stop         # 优雅关闭
```

> **说明**：GoalOS 的用户入口是 `goalos` 命令行（唯一软件入口）。daemon 的 HTTP API 是 CLI 的后端接口，经本机 Unix Socket 通信，不面向用户直接开放。

### 2.5 卸载

```bash
goalos daemon stop
rm /usr/local/bin/goalos-daemon /usr/local/bin/goalos
rm -rf ~/.goalos/     # 可选：删除系统状态
# 注意：~/Goals/ 目录中的产出物不受影响——它们是你的文件
```

---

## 3. 快速开始

### 3.1 第一个目标

```bash
goalos "帮我分析2024年AI行业趋势"
```

系统会：理解目标 → 生成任务计划（MissionGraph）→ **请你确认计划** → 逐项执行 → 交付结果到你的 workspace（默认 `~/Goals/<目标名>/`）。

### 3.2 查看进度

```bash
goalos goal list                 # 所有目标及状态
goalos goal status <id>          # 单个目标详情（中文状态）
goalos goal status <id> --json   # 机器可读（goal_id/visible_state/updated_at）
```

### 3.3 管理目标

```bash
goalos pause <id>      # 暂停
goalos resume <id>     # 恢复
goalos stop <id>       # 终止
goalos goal log <id>   # 查看审计日志
```

### 3.4 审批高风险操作

高风险操作会暂停等待你的决定（审批交互只在 CLI 呈现）：

```bash
goalos approval list                  # 查看待批事项（风险等级+操作描述）
goalos approve <id>                   # 批准
goalos reject <id>                    # 拒绝
goalos approve <id> --wait-more       # 我还没想好，延长审批窗口
```

多个待批事项时系统会提示你用 `--action <action_id>` 指定具体哪一个。审批超时（默认 5 分钟，可配置）自动拒绝——不会在你不在场时擅自执行。

### 3.5 查看审查意见

关键产出会经过多模型交叉验证。出现分歧时：

```bash
goalos review accept <goal>                     # 查看后接受结果
goalos review refine <goal> --feedback "..."    # 带反馈精修
```

### 3.6 失败恢复与调整

```bash
goalos retry <id> --feedback "换个思路"   # 新会话重做（保留已完成产物）
goalos adjust <id> --task "<描述>"        # 调整目标
goalos adjust <id> --budget 2000000       # 追加 Token 预算
goalos insert <id> --requirement "<文本>" # 追加需求
goalos confirm --goal <id>                # 确认临时 Flow 继续
```

### 3.7 安装 Skill

```bash
goalos skill list                 # 已安装 Skill
goalos skill info <name>          # Skill 详情
goalos skill install <market:name>  # 安装（技能市场随 v0.4.0 开放）
```

Skill 遵循 agentskills.io 开放标准——Markdown + YAML Frontmatter 声明式指令包。

---

## 4. 安全与治理

**所有执行必经安全检查**——Governance 五引擎在任务进入执行前逐一裁决，不可关闭、不可绕行：

| 引擎 | 说明 |
|------|------|
| Policy | 规则匹配（如"禁止删除生产文件"） |
| Capability | 能力校验（如"这个操作需要 shell.execute 能力"） |
| Risk | 风险评估（R0 无风险 ~ R5 关键风险） |
| Approval | 人工审批（策略/风险要求时触发——R3 及以上风险默认需你批准，R5 自动拒绝） |
| Audit | 全量审计。不可关闭 |

### 自治级别

| 级别 | 说明 |
|------|------|
| observe | 仅观察。不执行任何操作 |
| suggest | 建议模式。执行前提醒 |
| approve | 审批模式。R3+ 需人工批准（默认） |
| autonomous | 高自动。R0-R4 自动批准+全量审计+安全检测强制全开 |

```bash
goalos config set autonomy-level approve
```

> 提升自治级别（向 autonomous 方向）需要在命令行二次确认——防止误操作放大系统权限。

### 沙箱执行

所有插件在独立操作系统子进程中运行，受平台原生沙箱强制隔离（Linux: Landlock+seccomp / macOS: Seatbelt / Windows: Restricted Token+Job Object）。CLI 启动横幅会如实显示当前平台生效的安全级别——不伪造安全能力。

```bash
goalos sandbox doctor     # 沙箱能力诊断（你的平台支持到哪一级、依据是什么）
```

---

## 5. 命令参考

完整规范见 [04命令行界面设计规范](../开发文档/04命令行界面设计规范.md)。常用命令速查：

```bash
# 目标
goalos "<目标描述>"                    # 创建并执行
goalos goal create "<描述>" [--async]  # 创建（--async 后台执行）
goalos goal list / status / log        # 查看
goalos pause / resume / stop <id>      # 控制

# 审批与审查
goalos approve / reject <id>           # 审批决定
goalos approve <id> --wait-more        # 延长审批窗口
goalos approval list                   # 待批列表
goalos review accept / refine <goal>   # 审查决策

# 恢复与调整
goalos retry <id> [--feedback "..."]   # 失败重做（唯一恢复原语）
goalos confirm --goal <id>             # 确认计划继续
goalos adjust <id> --task / --budget   # 调整目标 / 预算
goalos insert <id> --requirement "..." # 追加需求

# 事件与审计
goalos events export <id> [--format jsonl]   # 导出事件投影
goalos goal log <id> [--format jsonl]        # 审计日志

# 配置
goalos config set autonomy-level <档>  # 自治级别
goalos config set persona <name>       # 系统语气（简洁/温暖/极简）
goalos config get <key>                # 查看配置

# 系统与诊断
goalos system status                   # 系统概览
goalos daemon status / restart / stop / logs
goalos sandbox doctor                  # 沙箱诊断
goalos storage doctor                  # 存储完整性诊断
goalos error explain <CODE>            # 错误码查询（如 SBX-EXE-R-001）
goalos metrics                         # Prometheus 指标
goalos --version / --help
```

---

## 6. 错误与排查

命令失败时输出尾部会附错误码——`goalos error explain <CODE>` 给出触发条件与行动指南（错误码知识库=09 规范，用户可见文案的唯一来源）。

### 退出码

| 退出码 | 含义 |
|--------|------|
| 0 | 成功 |
| 1 | 业务失败（目标以失败收场，已有产出物保留） |
| 2 | 用法错误（参数/格式） |
| 3 | 连接失败（daemon 未运行或无法启动） |
| 5 | 权限拒绝（能力或策略拒绝） |
| 7 | 状态不一致（竞速败者——请重新提交） |
| 124 | 执行超时 |
| 125 | 审批未获通过 / 审批超时 / 防御拦截 |

### 常见问题

| 现象 | 原因 | 处理 |
|------|------|------|
| Daemon 未运行 | 未启动或已退出 | 直接执行任意 `goalos` 命令自动启动；或 `goalos daemon restart` |
| Goal 创建后停留在"待确认" | 计划等待你确认 | `goalos confirm --goal <id>` |
| 一直"待决策" | 多模型审查有分歧 | `goalos goal log <id>` 查看详情后 `goalos review accept/refine` |
| Token 预算耗尽 | 消耗超过预算上限 | `goalos adjust <id> --budget <tokens>` 追加后 `goalos resume <id>` |
| 沙箱不可用 | 当前平台能力受限 | `goalos sandbox doctor` 查看诊断包；系统会如实标注降级级别 |
| `~/.goalos/` 权限错误 | 目录权限不足 | 检查 `~/.goalos/` 目录权限（密钥文件要求 0600） |

---

## 7. 数据与恢复

```
~/Goals/          # 你的产出物（文档、代码、报告）——普通文件，你拥有
~/.goalos/        # 系统状态（事件日志、配置、密钥）——可从事件日志重建
```

- **备份**：直接复制 `~/Goals/` 即可
- **崩溃恢复**：Daemon 重启后自动从事件日志（events.jsonl）恢复状态——已完成的 Action 不会重跑
- **失败恢复**：`goalos retry <id>` 以新会话重做——保留已完成产物，不重复扣费

---

## 8. 常见问题

**Q: GoalOS 需要联网吗？**
规划阶段需要 LLM（云端 API 或本地 Ollama）。已规划好的目标在断网下照常执行（本地沙箱路径无远程依赖）；使用本地模型时全链路可离线。

**Q: 我的数据在哪里？**
`~/Goals/<目标名>/`——普通文件。你可以用 Finder、VS Code、Git 操作它们。GoalOS 不锁定你的数据。

**Q: 支持哪些 LLM？**
Anthropic (Claude)、OpenAI、Google (Gemini)、本地 Ollama。通过配置切换；关键验收默认跨 Provider 交叉验证。

**Q: 如何贡献 Skill？**
Skill 遵循 agentskills.io 开放标准。编写 Markdown + YAML Frontmatter → 发布到技能市场（市场对接随 v0.4.0 开放）→ 其他用户可通过 `goalos skill install` 安装。

---

## 9. 参考

- 开发文档：[开发文档/](../开发文档/)
- 开发与安全规范：[11安全开发流程规范](../开发文档/11安全开发流程规范.md)
- 验收规范：[验收规范.md](验收规范.md)
- 开发日志：[开发日志.md](开发日志.md)

---

**GoalOS — 你只管提出目标。系统负责安全地实现它。**
